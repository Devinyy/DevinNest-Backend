version: '3.8'

services:
  backend:
    build: .
    container_name: devinnest-backend
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./static:/app/static
      - ./.env:/app/.env
    environment:
      - PYTHONPATH=/app
